<h1>Rename This Dispenser</h1>
<% if flash[:error] %>
  <div class="error flash-bar col flex-container site-wrapper--padding">
    <p class="flash-type col col--full col--center"><%= flash[:error] %></p>
  </div>
<% end %>

<%= form_for(@dispenser, url: dispenser_path(@dispenser), method: :patch) do |f| %>
<%= f.text_field :name, placeholder: @dispenser.name %>
<%= f.submit "Rename Dispenser" %>
<% end %>
<br><br>
<p>or</p>
<br><br>
<%= form_for(@dispenser, url: dispenser_path(@dispenser), method: :delete) do |f| %>
<%= f.submit "Delete this watering can" %>
<% end %>
